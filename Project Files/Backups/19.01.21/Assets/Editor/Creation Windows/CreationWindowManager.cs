using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CreationWindowManager : MonoBehaviour
{
    #region  Standard ItemSettings
    /*
    public static void FinishItemSetting()
    {

    }

    IEnumerator IEItemSettings()
    {
        Texture2D t = Resources.Load<Texture2D>("Prefabs/Basic Objects/" + parentName + "/" + longerName + "/" + projName + "_texture");
        Rect rt = new Rect(0, 0, t.width, t.height);
        img = Sprite.Create(t, rt, new Vector2(0.5f, 0.5f));
    }
    */
    #endregion
}
